import { NavLink } from "react-router-dom";

const Logo = () => {
  return (
    <NavLink className="text-2xl text-cyan-600 font-black" to="/">
      TheDarkArtist
    </NavLink>
  );
};

export default Logo;
